# ARCHITECTURE.md — قرارات التصميم

## لماذا Modular Architecture؟
كل مرحلة (استخراج، تنظيف، جلسة، دمج، تصدير) وحدة منفصلة بمجلدها الخاص، عشان خطأ بمرحلة وحدة ما ينهار باقي الخط.

## لماذا DataLY_OCR مشروع منفصل تماماً؟
تبعيات مختلفة (صور/OCR مقابل pandas/إكسل)، ونقطة فشل معزولة. الواجهة الوحيدة بينهم: ملف `invoice.xlsx` (بيانات، مو كود).

## لماذا categories.json بدل CATEGORY_MAP مكتوبة بالكود؟
عشان تصنيف واحد يُحدَّث بمكان واحد (بدل تكراره بمشروعين)، وبيانات التصنيف تصير قابلة للمراجعة بدون فهم بايثون.

## لماذا الكمية بالفاتورة = قطع مفردة، والصندوق كسري؟
قرار عمل مؤكد من صاحب المشروع (مو افتراض تقني): المورد يكتب الكمية كقطع فعلية، وعمود "الصندوق" مرجعي بس للمراجعة اللوجستية — كسوره مقبولة ومقصودة.

## لماذا تحقق (validators.py) بدل الاستمرار الصامت؟
بيانات غلط تدخل مخزون تجاري حقيقي أخطر من توقف الكود وطلب تدخل بشري. لو عمود أساسي مفقود، البرنامج يوقف برسالة عربية واضحة بدل ما يكمل بأرقام صفرية مضلِّلة.

## لماذا Tesseract بدل Claude API بمرحلة OCR؟
قرار مؤقت بسبب عدم توفر ميزانية API حالياً. التصميم يفصل "استخراج النص" (`_ocr_with_confidence`) عن "تفسيره" (`_parse_line`)، عشان تبديل محرك OCR لاحقاً (لـ Claude Vision مثلاً) ما يكسر باقي الأداة.

## لماذا نظام ثقة + مراجعة (Review Queue) بدل الوثوق الكامل بـ OCR؟
Tesseract أضعف من نموذج رؤية متقدم على صور واتساب الحقيقية (زوايا، إضاءة، خط يد). بدل تخمين بيانات غلط بصمت، أي سطر بثقة واطية يُحوَّل لملف مراجعة يدوية منفصل — نفس فلسفة `unclassified.xlsx` بالتصنيف.

## لماذا خيار المراجعة اليدوية للتصنيف بدل استدعاء API؟
لا توجد ميزانية API حالياً. صنف يُصنَّف يدوياً **مرة واحدة فقط**، ثم يُحفظ بالذاكرة الدائمة للأبد — التكلفة اليدوية تتضاءل مع الوقت مثل ما كان مخطط للتصنيف عبر API بالضبط.
