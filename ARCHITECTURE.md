# ARCHITECTURE.md — قرارات التصميم

## لماذا Modular Architecture؟
كل مرحلة (استخراج، تنظيف، جلسة، مطابقة، دمج، تصدير) وحدة منفصلة بمجلدها الخاص، عشان خطأ بمرحلة وحدة ما ينهار باقي الخط.

## لماذا categories.json بدل CATEGORY_MAP مكتوبة بالكود؟
عشان تصنيف واحد يُحدَّث بمكان واحد، وبيانات التصنيف تصير قابلة للمراجعة بدون فهم بايثون.

## لماذا الكمية بالفاتورة = قطع مفردة، والصندوق كسري؟
قرار عمل مؤكد من صاحب المشروع (مو افتراض تقني): المورد يكتب الكمية كقطع فعلية، وعمود "الصندوق" مرجعي بس للمراجعة اللوجستية — كسوره مقبولة ومقصودة.

## لماذا تحقق (validators.py) بدل الاستمرار الصامت؟
بيانات غلط تدخل مخزون تجاري حقيقي أخطر من توقف الكود وطلب تدخل بشري. لو عمود أساسي مفقود، البرنامج يوقف برسالة عربية واضحة بدل ما يكمل بأرقام صفرية مضلِّلة.

## لماذا OCR مؤجَّل؟
كان فيه مشروع OCR منفصل (`DataLY_OCR/`) خارج هذا المجلد، تم حذفه لاحقًا بدون أي تأثير على هذا المشروع (الواجهة الوحيدة بينهم كانت ملف بيانات، مو كود مشترك). الحجم الحالي من الفواتير لسا يدوي وقابل للمتابعة، فالاعتماد على قراءة الصور يدويًا وقت الحاجة بدل بناء محرك OCR كامل قبل أوانه.

## مطابقة الأصناف (fusion/reconciliation.py) — لماذا الباركود أولاً؟
نفس الصنف يُكتب بصيغ مختلفة من مورد لآخر ("أرز أبيض ممتاز 5 كجم" مقابل "رز ابيض فاخر 5ك")،
وهذا يُنتج بطاقات صنف مكررة بمنظومة السهل لنفس المنتج الفعلي. الباركود هوية فيزيائية للمنتج
لا تتأثر بصياغة الاسم، فهو مصدر المطابقة الأساسي. **التوقيت مقصود: بعد** أن يعبّي التاجر
الباركود بواجهة `receiving_app.py` (Streamlit)، ما قبلها — عشان ما نعقّد جلسة التاجر بخطوة
مطابقة إضافية أثناء الاستلام.

- **باركود موجود بـ `data/master_items.xlsx`** → "تطابق مُقترَح" (اسم + تصنيف من master)،
  **بانتظار موافقتك دائمًا** — مهما كانت نسبة التشابه، ما فيه استبدال تلقائي إطلاقًا (قرار
  متعمَّد، راجع القسم اللي تحت). عند الموافقة، تُدمج الكمية تلقائيًا لو نفس الفاتورة فيها
  صفّين وصلوا لنفس الباركود بصياغتين مختلفتين.
- **باركود غير موجود بـ master** → صنف جديد فعلًا، يُسجَّل تلقائيًا فورًا بنفس الاسم المكتوب
  بالفاتورة — **بدون انتظار موافقة**، لأن هذا إضافة جديدة، مو تعديل على شي معتمد من قبل.
- **باركود غير مُدخل إطلاقًا** → fuzzy matching (`rapidfuzz`) على الاسم كخط دفاع احتياطي فقط،
  بحد ثقة أعلى من حالة الباركود المؤكد (لأنه بدون باركود ما فيه مرساة هوية موثوقة)، ولو تجاوز
  الحد يرجع بردو كـ"تطابق مُقترَح" بانتظار موافقتك — نفس معاملة تطابق الباركود بالضبط.
- **أي تطابق فيه شك** (تشابه اسم منخفض، أو حجم/وزن مختلف) → يُعرض بردو للموافقة، لكن مع
  "تنبيه" واضح يشرح سبب الشك، عشان تاخذ قرارك وأنت شايف كل المعلومة.

**لماذا مقياس WRatio (rapidfuzz) بدل token_sort_ratio العادي؟** نفس الصنف بصياغتين عربيتين
مختلفتين (زيادة/نقصان همزة، اختصار الوحدة) يعطي تشابه منخفض بمقاييس المسافة الحرفية البسيطة رغم
كونه نفس المنتج المؤكد بالباركود، بينما WRatio يميّز بوضوح بين هذا التفاوت الطبيعي بالصياغة
وبين تعارض حقيقي (صنف مختلف كليًا وصل لنفس الباركود بالخطأ).

**لماذا ملف واحد `master_items.xlsx` بدون معامل `store_id`؟** لا يوجد مفهوم "متجر" بالكود
حاليًا، وكل نشر (deployment) من DataLY يخدم متجر واحد بمجلد `data/` خاص فيه — يعني "لكل متجر"
يتحقق تلقائيًا بفصل النشر، بدون طبقة فهرسة إضافية. لو احتجنا تعدد متاجر بنفس التثبيت لاحقًا،
يُضاف معامل `store_id` وقتها.

**لماذا مرونة أعمدة التصنيف بـ master؟** قد لا تحتوي قاعدة الأصناف المستلمة من المتجر تفاصيل
تصنيف معتمدة لكل صنف. لو كانت فارغة، يبقى تصنيف `categorizer.py` (بالكلمات المفتاحية) كما هو
بدل استبداله بقيمة فارغة.

**لماذا التعرّف التلقائي على أسماء أعمدة master؟** سير العمل الفعلي: بكل زيارة لمتجر، يُستبدل
`data/master_items.xlsx` بملف قاعدة أصناف ذلك المتجر المُصدَّر من منظومته الحالية (السهل/سراج/
كريستال)، وكل منظومة تسمّي الأعمدة بشكل مختلف — مثلًا ملفات فاتورة مشتريات الحقيقية بمنظومة
السهل تستخدم "الكود" للباركود و"الوصف" للاسم، مو "الباركود"/"اسم الصنف". `load_master()` بـ
`fusion/reconciliation.py` يتعرّف على المتغيرات الشائعة تلقائيًا (`MASTER_POSSIBLE_COLUMNS`)،
بنفس أسلوب `POSSIBLE_COLUMNS` بـ `extractors/excel_extractor.py`، وبقائمة واسعة عمدًا
(مرادفات عربية وإنجليزية كثيرة لكل عمود) لأن كل منظومة تسمّي نفس العمود بشكل مختلف.
**كلا العمودين إلزاميان:** لو تعذّر إيجاد عمود اسم الصنف أو عمود الباركود/الكود إطلاقًا،
يوقف البرنامج برسالة عربية واضحة (`MasterDataError`) بدل الاستمرار الصامت بملف عديم الفائدة —
المطابقة بالكامل مبنية على الباركود، فملف بدونه لا قيمة حقيقية له.

## لماذا موافقة بشرية على *كل* تطابق، مو بس الحالات المتعارضة؟
قرار متعمَّد من صاحب المشروع بعد تجربة ميدانية: حتى تطابق بثقة عالية نصيًا ممكن يكون غلط
(راجع القسم اللي تحت — WRatio وحده ما كفى). فبدل تصنيف تلقائي "مقبول/مرفوض" بحد ثقة، **كل
تطابق بالباركود أو بالاسم التقريبي يرجع كـ"تطابق مُقترَح" فقط، وينتظر قرارك أنت** — موافقة،
رفض، أو تسمية يدوية. الاستثناء الوحيد: **صنف بباركود جديد كليًا يُسجَّل تلقائيًا** بدون
انتظار موافقة، لأنه إضافة بحتة، ما فيه شي معتمد سابقًا يتغيّر أو يُستبدل.

`resolve_matches_interactively()` بـ `fusion/reconciliation.py` تعرض كل تطابق بالطرفية وقت
`python main.py --merge` مباشرة (بدل ملف مراجعة منفصل تكتشفه لاحقًا)، مع "تنبيه" واضح لو فيه
شك (تشابه اسم منخفض، أو حجم/وزن مختلف — راجع القسم اللي تحت). التسمية اليدوية تُحدَّث
بالفاتورة **و**بـ`master_items.xlsx` معًا — نفس فلسفة "يُصنَّف مرة وحدة ويُحفظ للأبد": لو
صححته الآن، ما يرجع يسألك عنه بنفس الباركود مرة ثانية. الدالة منفصلة عمدًا عن
`reconcile_dataframes()` (اللي تبقى pure بدون أي تدخل بشري، قابلة للاختبار بالكامل — ترجع
التطابقات المُقترَحة فقط بدون تطبيقها)، وتتعامل بأمان مع تشغيل غير تفاعلي (`EOFError`)
بافتراض "رفض" (الاسم الأصلي) تلقائيًا بدل ما تعلّق بانتظار إدخال ما راح يجي.

## لماذا فحص الحجم/الوزن بجانب WRatio؟
اكتُشفت بتجربة ميدانية حقيقية: "سكر ناعم 10 كجم" و"سكر التميز 1 كغ" طلعوا متشابهين نصيًا
(67% بمقياس WRatio) رغم إن الحجم مختلف 10 أضعاف فعليًا — WRatio يقيس تشابه النص الكلي بس،
وكلمة "سكر" المشتركة كانت كافية تطغى على فرق الرقم. الحل: استخراج الوزن (غرام) أو الحجم
(مليلتر) من الاسمين ومقارنتهم كفحص مستقل، يُضاف كـ"تنبيه" على التطابق المُقترَح (مو رفض
تلقائي — القرار النهائي دايمًا لك):
- نفس فئة القياس (وزن مع وزن، أو حجم مع حجم) لكن النسبة بينهم ≥ `SIZE_MISMATCH_RATIO` (1.3)
  → تنبيه "الحجم/الوزن مختلف كليًا".
- فئة القياس نفسها مختلفة (وزن مقابل حجم، زي "5 كجم" مقابل "250 مل" — حالة حقيقية ثانية
  اكتُشفت بنفس التجربة) → تنبيه "نوع القياس نفسه مختلف"، حتى بدون مقارنة الأرقام رياضيًا.
- تعذّر استخراج حجم من أحد الاسمين (اختصار غامض زي "5ك") → بلا تنبيه حجم، تشابه الاسم
  (WRatio) وحده يبقى المعيار المعروض.

## لماذا خيار المراجعة اليدوية للتصنيف بدل استدعاء API؟
لا توجد ميزانية API حالياً. صنف يُصنَّف يدوياً **مرة واحدة فقط** (سواء بالتصنيف أو بمطابقة الأصناف)، ثم يُحفظ بالذاكرة الدائمة (`categories.json` أو `master_items.xlsx`) للأبد — التكلفة اليدوية تتضاءل مع الوقت.
