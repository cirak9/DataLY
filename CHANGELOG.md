# CHANGELOG.md

## تطبيق الويب — النشر الحي الأول (2026-09-09/10)

هذا القسم يوثّق تطبيق الويب (`backend/` + `frontend/`) بشكل منفصل عن ترقيم إصدارات
الأداة الأصلية لسطر الأوامر تحت — نظام إصدار مستقل، مو استمرار لـ v6.1.1.

- 🆕 خط الأنابيب الكامل مبني ومنشور فعلياً: رفع فاتورة → تنظيف/تصنيف تلقائي → جلسة
  استلام تاجر → تسوية أصناف → دمج بالمخزون التراكمي → تصدير Alsahl. تسع شاشات، كلها
  متصلة بالباك إند الحقيقي، صفر placeholders.
- 🆕 نشر حي كامل: الواجهة على Vercel (https://data-ly.vercel.app)، الباك إند على
  Render (https://dataly-backend.onrender.com)، قاعدة البيانات Postgres على Supabase.
- 🆕 تصميم متجاوب على كل الشاشات ذات الجداول: قائمة بطاقات عمودية على الموبايل
  (`sm:hidden`)، جدول كامل على تابلت/سطح مكتب (`sm:block`) — بدل تمرير أفقي مزعج
  لجدول بعرض حاسوب على شاشة هاتف. متحقَّق يدوياً على 375×812 و768×1024 وسطح المكتب.
- 🔴 إصلاح: `GET /invoices/{id}/export/download` كان يفشل بـ500 من الرابط الحي فقط
  (يشتغل صح محلياً) — قرص Render المجاني مؤقت وينمسح بإعادة النشر، فملف `.xlsx`
  المحفوظ وقت التصدير يختفي بعدها. `FileResponse` كان يفشل باستثناء غير معالَج يهرب
  من `CORSMiddleware`، فيوصل للمتصفح كـ"CORS policy" مضلِّل بدل الخطأ الحقيقي. الحل:
  التنزيل يُعاد بناؤه من قاعدة البيانات مباشرة بكل مرة (`build_export_bytes()`) بدل
  الاعتماد على وجود ملف فعلي على القرص.
- 🔴 إصلاح: دخول مباشر (تحديث صفحة، رابط مُرسَل) لأي مسار متداخل زي
  `/stores/3/history` كان يرجع 404 من Vercel — أُضيف `vercel.json` بـrewrite يسلّم
  `index.html` لكل المسارات ويخلّي React Router يتولى التوجيه.
- 🧹 توثيق: `README.md`/`backend/README.md`/`frontend/README.md` مُحدَّثة بالروابط
  الحية، المكدس التقني، وقرارات التصميم — بعد ما كانت تصف حالة "قيد الإعادة بناء"
  وشاشات "ComingSoon" غير موجودة فعلياً.
- 🔴 إصلاح أمان جوهري: كل مستخدم مسجّل دخول كان يشوف **كل** المتاجر (بياناتهم، فواتيرهم،
  مخزونهم) بغض النظر عمّن أنشأها — عيب تصميم خطير لأي استخدام حقيقي متعدد العملاء.
  أُضيف `stores.owner_id` (migration `0003`) وكل نقاط الـAPI المتفرعة من متجر (فواتير،
  جلسات، تسوية، دمج، تصدير، مخزون) صارت تتحقق من الملكية عبر دوال `get_owned_*` جديدة
  بـ`app/api/deps.py` — 404 لا 403 لو المتجر موجود بس مو مملوك للمستخدم الحالي، عشان
  ما نسرّب حتى معلومة وجوده. `product_catalog`/التصنيفات/الموردون يبقون مشتركين عمداً
  (فهرس عام، مو بيانات تجارية). متحقَّق حياً: كل حساب يشوف متجره بس، بالاتجاهين.
- 🧪 44 اختبار pytest آلي جديد يغطّون العزل (`test_store_isolation.py`) — كل نقطة API
  الـ13 المتفرعة من متجر، بالاتجاهين، بما فيها التفريق المتعمَّد بين 404 (رفض ملكية)
  و422 (رفض حالة عمل) لإثبات إن التحقق من الملكية هو اللي يسمح/يمنع، مو صدفة.
- 🆕 استيراد المخزون القديم: `POST /stores/{id}/inventory/import` — يقرأ ملف مخزون
  حالي (باركود/اسم/صلاحية إجباري، كمية/تكلفة وحدة اختياري) بنفس كشف الأعمدة الذكي من
  الأداة الأصلية، ويدمجه بـ`inventory_lots` (نفس باركود+صلاحية = استبدال بالأحدث). يُسأل
  التاجر قبل كل جلسة استلام جديدة بالواجهة (خطوة اختيارية، النظام ما يقدر يكتشف إضافات
  يدوية تلقائياً). بق حقيقي انكشف وانصلح بالطريق: باركودات رقمية بعمود فيه خلايا فاضية
  كانت تطلع بلاحقة `.0` زايدة من pandas (`123` ← `123.0`) — ما تطابق الباركود الحقيقي
  بأي مكان ثاني بالنظام.
- 🆕 تسجيل تاجر ذاتي: شاشة `/register` جديدة (اسم متجر + رقم هاتف + كلمة مرور) عبر
  `POST /auth/register` — تنشئ الحساب والمتجر المملوك له بنفس اللحظة، وترجّع توكن
  جاهز (دخول تلقائي، بلا خطوة منفصلة). `users.email` صار اختياري (الحسابات الإدارية
  القديمة تشتغل بالبريد زي ما هي)، وشاشة الدخول تقبل بريد أو هاتف بنفس الحقل. تغيير
  جوهري بآلية الـJWT بنفس الطريق: التوكن صار مبني على `user.id` بدل البريد — كل جلسة
  دخول قديمة (توكنات مخزّنة بالمتصفح) بتنتهي صلاحيتها فوراً بعد هالنشرة وتحتاج دخول
  من جديد (متوقَّع، مو خطأ). متحقَّق حياً بالكامل: تسجيل، دخول بالهاتف، عزل المتجر
  الجديد عن باقي الحسابات، ودخول الحسابات القديمة بالبريد ما انكسر.
- 🆕 الكمية صارت قابلة للتعديل بشاشة مراجعة الفاتورة (`PATCH /invoices/{id}/items/{item_id}`
  يقبل `quantity_pieces` الآن) — كانت محصورة بالاسم وتكلفة الوحدة بس، رغم إن الكمية
  نفسها عرضة لخطأ الاستخراج التلقائي زي أي حقل ثاني.
- 🆕 استخراج فاتورة من صور عبر Claude Vision — مسار كامل ثلاث خطوات: `POST
  /stores/{id}/invoices/ocr` (رفع صورة أو أكثر، فاتورة `method=3` جديدة)، `POST
  /invoices/{id}/ocr-extract` (استخراج الأصناف بدون حفظها — اسم/كمية/سعر
  وحدة/باركود مع درجة ثقة لكل صنف)، شاشة مراجعة/تعديل حر بالواجهة
  (`/invoices/{id}/ocr-review`)، ثم `POST /invoices/{id}/ocr-confirm` — الاعتماد
  الصريح اللي يحوّل الأصناف المراجَعة فعلياً لـ`invoice_items` وينقل الفاتورة
  لحالة "cleaned"، تكمل بعدها بنفس مسار الفواتير العادية (جلسة استلام ← تسوية ←
  دمج ← تصدير) بلا فرق. جُرِّب مسار Tesseract أولاً (خدمة Render بـDocker منفصلة)
  لكن أُلغي لصالح الخطة الأصلية — استدعاء HTTP عادي، يشتغل بنفس الخدمة الحية
  الحالية بلا أي تغيير بنية. `ANTHROPIC_API_KEY` لسا مو مُعدّ بالاستضافة —
  `ocr-extract` يرجّع 503 واضح بدلاً من خطأ غامض لحد ما يُضاف يدوياً لاحقاً.

## v6.1.1 — تدقيق مركّز على categories.json
- 🔴 إصلاح: "رز" بدون whole_word كانت تتطابق داخل "كرز" — مربى/عصير الكرز يترجّح خطأً "أرز وحبوب".
- 🔴 إصلاح: كلمة "مسحوق" المجرّدة كانت تصنّف أي منتج مسحوق (حليب أطفال، بروتين، بيكنج باودر)
  كـ"منظفات منزلية/غسيل ملابس" — أُزيلت لصالح "مسحوق غسيل" الأدق (موجودة أصلًا بنفس القائمة).
- 🟡 تقوية: "ماي" صارت whole_word — كانت تعتمد بالصدفة على ترتيب الفئات بالملف عشان ما
  تتطابق داخل "مايونيز" (لأن فئة صلصات ومعلبات مكتوبة قبل فئة مياه بالملف حاليًا).
- 🆕 اختُبر الكود على فاتورة حقيقية بـ100 صنف مواد غذائية متنوّعة — 7 أصناف طلعت "أخرى"
  (شعيرية، برغل، بيكنج باودر، مربى، عسل، زيتون، بروتين)، فأُضيفت 3 تصنيفات فرعية جديدة
  ("مربيات وعسل"، "زيتون ومخللات"، "مكملات غذائية") وكلمتين لتصنيفين قائمين — الفاتورة
  نفسها صارت 100/100 مصنَّفة بدون أي "أخرى". "عسل" صارت whole_word (خطر تصادم مع "معسل").
- 🆕 7 اختبارات جديدة بـ`test_categorizer.py` (34 اختبار إجمالًا).

## v6.1.0
- 🆕 `fusion/reconciliation.py` — خطوة توحيد الأصناف عبر الباركود مقابل `data/master_items.xlsx`
  (يتعرّف تلقائيًا على تسمية أعمدة أي منظومة)، بمطابقة تقريبية (rapidfuzz) كخط دفاع احتياطي
  عند غياب الباركود، وفحص حجم/وزن مستقل عن تشابه النص.
- 🆕 `python main.py --merge` يشغّل التوحيد تلقائيًا قبل الدمج والتصدير. أي تطابق (بالباركود
  أو بالاسم) يُعرض بالطرفية لموافقة صريحة دائمًا — قبول، رفض، أو تسمية يدوية — بدل استبدال
  تلقائي على الحالات عالية الثقة.
- 🧹 `data/master_items.xlsx` **للقراءة/المقارنة فقط** — لا يُنشأ ولا يُعدَّل ولا يُحفظ من
  الكود إطلاقًا (لا تسجيل أصناف جديدة، ولا حفظ تسميات يدوية فيه). قرار صريح من صاحب المشروع:
  كل تشغيلة مستقلة، يُحضِر هو نسخة حديثة من قاعدة الأصناف يدويًا كل مرة.
- 🧹 إزالة النسخ الاحتياطية التلقائية (`data/backups/`)، ملف سجل قرارات المطابقة، وملف
  `logs/dataly.log` — نفس فلسفة "بلا حفظ تقدّم بين تشغيلة وأخرى"، طباعة بالطرفية فقط.
- 🆕 مجلد tests/ — اختبارات `test_reconciliation.py` (29 اختبار).
- 🧹 تنظيف توثيق: إزالة إشارات `DataLY_OCR` و Tesseract من ARCHITECTURE.md/README.md بعد حذف
  ذاك المشروع فعليًا (OCR مؤجَّل عمدًا، بدون تأثير على هذا المشروع).

## v6.0.0
- 🔴 إصلاح: عمود "تكلفة الوحدة" كان يتصفّر بعد الدمج (تعارض اسم عمود بين invoice_data وsession_output)
- 🟠 إصلاح: تطبيق نسبة الخصم على حسابات التكلفة (كانت متجاهَلة تماماً)
- 🟡 إصلاح: كود الصنف من المورد يُستخدم كـ fallback لو ما فيه باركود من التاجر
- 🆕 categories.json كمصدر وحيد للتصنيفات (بدل تكرارها بالكود) + إصلاح خلل "صن" داخل "صنف"
- 🆕 utils/validators.py — توقف واضح لو أعمدة أساسية مفقودة أو بيانات سالبة
- 🆕 utils/logger.py — تسجيل كامل لكل تشغيلة بـ logs/dataly.log
- 🆕 نسخ احتياطية تلقائية لـ invoice_data.xlsx وsession_output.xlsx قبل كل دمج (data/backups/)
- 🆕 مجلد tests/ باختبارات pytest (11 اختبار)
- 🆕 ترقيم إصدار صريح (VERSION، ARCHITECTURE.md، هذا الملف)

## v5 وما قبلها
- النسخة الأصلية بدون الإصلاحات أعلاه — راجع محادثات التطوير للتفاصيل.
