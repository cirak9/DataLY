# دليل الاستخدام — DataLY v7.0.0

## 📖 مقدمة

**DataLY** — نظام معالجة فواتير الموردين وتصديرها لمنظومة السهل، يدعم طريقتين مختلفتين لاستقبال البضاعة:

- **الطريقة الأولى**: إدخال يدوي كامل (باركود + صلاحية + سعر البيع)
- **الطريقة الثانية**: إدخال سريع (سعر البيع فقط) — أسرع بـ 3 مرات ⚡

---

## 🚀 البدء السريع

### التثبيت

```bash
cd DataLY
pip install -r requirements.txt
```

### الملفات المطلوبة

لكل متجر، ستحتاج إلى:
```
data/
└── store_name/
    ├── old_inventory.xlsx       ← جرد المخزن القديم
    ├── invoice.xlsx             ← فاتورة المورد
    └── supplier_inventory.xlsx  ← جرد المورد (اختياري)
```

---

## 📋 الطريقة الأولى (إدخال يدوي)

### متى تستخدمها؟
- عندما **لا تملك** جرد مخزون من المورد
- عندما تريد **إدخال يدوي كامل**
- عندما تحتاج **تحكم أكبر** بالبيانات

### الخطوات

**1️⃣ معالجة الفاتورة:**
```bash
python main.py \
  --store store_1 \
  --inventory data/store_1/old_inventory.xlsx \
  --invoice data/store_1/invoice.xlsx
```

**2️⃣ فتح واجهة الاستلام:**
```bash
streamlit run session/receiving_app.py
```

**3️⃣ تعبئة البيانات:**
```
للكل صنف:
  🔍 الباركود:     [001234567890]
  📅 الصلاحية:     [06/2027]
  🏷️  سعر البيع:    [15.99]
```

**4️⃣ تحميل النتيجة:**
```
بعد إكمال جميع الأصناف:
  📥 [تحميل session_output.xlsx]
```

**5️⃣ دمج وتصدير:**
```bash
python main.py --merge --store store_1
```

---

## ⚡ الطريقة الثانية (إدخال سريع)

### متى تستخدمها؟
- عندما **تملك** جرد مخزون من المورد
- عندما تريد **تسريع العملية** ⚡
- عندما تريد **تقليل الأخطاء**

### الخطوات

**1️⃣ معالجة الفاتورة:**
```bash
python main.py \
  --store store_1 \
  --inventory data/store_1/old_inventory.xlsx \
  --invoice data/store_1/invoice.xlsx \
  --supplier data/store_1/supplier_inventory.xlsx
```

**2️⃣ فتح واجهة الاستلام:**
```bash
streamlit run session/receiving_app.py
```

**3️⃣ تعبئة السعر فقط:**
```
للكل صنف:
  🏷️  سعر البيع:    [15.99]
  
(الباركود والصلاحية معبأة مسبقاً)
```

**4️⃣ تحميل النتيجة:**
```
بعد إكمال جميع الأصناف:
  📥 [تحميل session_output.xlsx]
```

**5️⃣ دمج وتصدير:**
```bash
python main.py --merge --store store_1
```

---

## 📊 صيغ الملفات

### جرد المخزن القديم
```
old_inventory.xlsx

الأعمدة المطلوبة (بأي ترتيب):
  • باركود / كود / code / barcode
  • اسم الصنف / الاسم / name / product_name / وصف
  • صلاحية / انتهاء / expiration / expiry / تاريخ الانتهاء

مثال:
┌─────────┬──────────────────────┬──────────┐
│ باركود  │ اسم الصنف            │ صلاحية  │
├─────────┼──────────────────────┼──────────┤
│ 001     │ أرز أبيض (5 كجم)    │ 2026-12  │
│ 002     │ سكر ناعم (1 كجم)    │ 2027-03  │
└─────────┴──────────────────────┴──────────┘
```

### جرد المورد
```
supplier_inventory.xlsx

الأعمدة المطلوبة (نفس صيغة المخزون):
  • باركود
  • اسم الصنف
  • صلاحية

ملاحظة: يُستخدم فقط للطريقة الثانية
```

### فاتورة المورد
```
invoice.xlsx

الأعمدة المطلوبة (الحد الأدنى):
  • item_name / الاسم / البيان
  • boxes / الكمية
  
الأعمدة الاختيارية:
  • unit / العبوة
  • cost_price / total_price / السعر
  • category / التصنيف
```

---

## 🎯 الخيارات المتقدمة

### فرض طريقة معينة
```bash
# الطريقة الأولى (تجاهل المورد إن وجد)
python main.py \
  --store store_1 \
  --inventory ... \
  --invoice ... \
  --method 1

# الطريقة الثانية (إجبار)
python main.py \
  --store store_1 \
  --inventory ... \
  --invoice ... \
  --supplier ... \
  --method 2
```

### معالجة متاجر متعددة
```bash
# المتجر الأول
python main.py --store store_1 --inventory ... --invoice ...

# المتجر الثاني
python main.py --store store_2 --inventory ... --invoice ...

# دمج كل متجر على حدة
python main.py --merge --store store_1
python main.py --merge --store store_2
```

---

## 📂 هيكل المشروع

```
DataLY/
├── extractors/               ← استخراج البيانات
│   └── inventory_extractor.py
├── transformers/             ← تنظيف وتحقق
│   └── base_cleaner.py
├── fusion/                   ← معالجة متقدمة
│   ├── method_router.py      ← اختيار الطريقة
│   ├── inventory_manager.py  ← إدارة المخزون
│   ├── inventory_matcher.py  ← مطابقة الطريقة الأولى
│   └── supplier_matcher.py   ← إثراء الطريقة الثانية
├── pipelines/                ← خطوط العمل
│   ├── method_1_pipeline.py
│   └── method_2_pipeline.py
├── session/                  ← واجهة الاستلام
│   └── receiving_app.py
├── adapters/                 ← التصدير
│   └── alsahl_adapter.py
├── utils/                    ← أدوات مساعدة
│   ├── categorizer.py
│   ├── validators.py
│   └── logger.py
├── data/                     ← ملفات العمل
│   ├── store_1/
│   ├── store_2/
│   └── master_items/
├── output/                   ← الملفات النهائية
├── main.py                   ← البرنامج الرئيسي
└── requirements.txt
```

---

## ⚙️ الإعدادات

### المتطلبات الأساسية
```
python >= 3.8
pandas >= 1.3.0
openpyxl >= 3.6.0
streamlit >= 1.0.0
rapidfuzz >= 2.0.0
```

### التثبيت الكامل
```bash
pip install -r requirements.txt
```

---

## 🐛 استكشاف الأخطاء

### الخطأ: "لم يتم العثور على عمود..."
**الحل:** تأكد من أسماء الأعمدة بالملف، أضفها لـ `POSSIBLE_COLUMNS` إذا كانت مختلفة

### الخطأ: "جرد المخزن فارغ"
**الحل:** هذا طبيعي للعملية الأولى — سيتم استخدام الفاتورة كمخزون ابتدائي

### الخطأ: "ملف الجلسة غير موجود"
**الحل:** شغّل `python main.py` أولاً قبل `streamlit run session/receiving_app.py`

### الخطأ: "لا يوجد تطابقات"
**الحل:** تحقق من الباركود بالمخزون، قد تكون هناك فروقات في الكتابة

---

## 📊 المخرجات

### 1. جلسة الاستلام
```
session_template.xlsx
├── item_id        ← معرف الصنف
├── الصنف          ← اسم الصنف
├── الباركود       ← الباركود
├── الصلاحية       ← تاريخ الصلاحية
└── سعر البيع      ← السعر
```

### 2. ملف الجلسة المكتمل
```
session_output.xlsx
├── item_id        ← معرف الصنف
├── الصنف          ← الاسم النهائي
├── الباركود       ← الباركود المدخل
├── الصلاحية       ← الصلاحية المدخلة
├── سعر البيع      ← السعر المدخل
└── مكتمل          ← حالة الإكمال
```

### 3. الملف النهائي
```
output_alsahl.xlsx
(جاهز للتصدير لمنظومة السهل)
```

---

## 💡 نصائح وأفضليات

✅ **الأفضل:**
- استخدم الطريقة الثانية عندما تملك جرد المورد
- راجع البيانات بعناية قبل التصدير
- احفظ نسخة من جرد المخزون الأخير

❌ **تجنب:**
- تعديل ملفات المخزون يدويًا أثناء التشغيل
- استخدام أسماء ملفات بحروف غير عربية
- تشغيل عمليات متزامنة على نفس المتجر

---

## 📞 الدعم

### للمزيد من المعلومات:
- راجع `ARCHITECTURE.md` للتفاصيل التقنية
- راجع `CHANGELOG.md` لسجل التحديثات
- راجع `README.md` للبدء السريع

---

**الإصدار:** 7.0.0  
**التاريخ:** 2026-08-25  
**الحالة:** ✅ جاهز للاستخدام
